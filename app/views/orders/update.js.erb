$("ul.errors").html("")
<% if @order.errors.any? %>
  <% @order.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>
  $("#cart-index").html("<%= j render "cart" %>")
  $("#cart-modal").modal("hide")
<% end %>
