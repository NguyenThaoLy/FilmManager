<% if @schedule.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <%= t ".contains" %> <%= pluralize @schedule.errors.count, t(".error") %>.
    </div>
    <ul>
    <% @schedule.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
